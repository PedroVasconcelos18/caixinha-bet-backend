/**
 * Adapters de persistência do módulo Pagamento: entidades JPA e repositórios
 * Spring Data. Separado de {@code adapter.asaas} (PSP) para distinguir
 * fronteiras de infraestrutura. O domínio nunca conhece este pacote
 * (ArchUnit guarda).
 */
package com.caxinhabet.pagamento.adapter.persistence;
